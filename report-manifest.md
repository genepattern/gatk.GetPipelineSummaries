Generated manifest for GATK.GetPileupSummaries v1 using Docker image broadinstitute/gatk:4.1.4.1. The module wraps GATK4 GetPileupSummaries via a bash wrapper script (gatk_getpileupsummaries_wrapper.sh) that stages all input files (BAM+BAI, VCF+TBI, intervals+index) into the writable job working directory before invoking GATK, and cleans up staged files via a trap on EXIT. Key fix from attempt 1: p7 output.file.name has prefix_when_specified='' (empty) because the --output.file.name flag is already inline in the commandLine template, preventing the duplicate-flag error. The optional arguments.file parameter (p8) uses prefix_when_specified='--arguments.file ' so the flag is only added when the user supplies the file.